package com.breakinblocks.mienergytiers.power;

/** Reserved policy hook. RESET_AND_WASTE_INPUTS is deliberately not configurable yet. */
public enum UnderpowerPolicy {
    PRESERVE_PROGRESS,
    RESET_AND_WASTE_INPUTS
}
